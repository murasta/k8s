apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: python-api
  labels:
    projekt: python
spec:
  ingressClassName: nginx
  rules:
  - host: python.209.38.243.80.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: python-service
            port:
              number: 80
 